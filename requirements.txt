# Core dependencies for CoreQ.
# Pinned versions match those used to produce the results reported in the paper.
# CUDA 12.4 wheels were used for torch / triton; adjust the index URL as needed.

# Deep learning
torch==2.5.0
torchaudio==2.5.0
torchvision==0.20.0
triton==3.5.1

# Hugging Face stack
transformers==4.51.0
accelerate==1.10.1
huggingface-hub==0.34.0
safetensors==0.6.2
datasets==3.6.0
sentencepiece==0.2.0
tokenizers==0.21.4

# Evaluation
lm_eval==0.4.9.1

# Optional incoherence pre-processing (Hadamard rotations)
fast_hadamard_transform @ git+https://github.com/Dao-AILab/fast-hadamard-transform@4ea722e434e3d4f2a14522341959ebdbe62be2de

# Numerics / utilities
numpy==1.26.4
scipy==1.16.2
matplotlib==3.10.7
texttable==1.7.0
tqdm==4.67.1

# Logging (optional, only used when --wandb is passed)
wandb==0.16.3
